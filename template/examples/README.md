# Examples and fixtures

Place small, reviewable examples here when they help humans or agents understand the skill's contracts.

Recommended groups:

```text
examples/
├── valid/
├── invalid/
└── expected/
```

Do not use examples as the only specification. Machine validation and tests should enforce the same rules.
