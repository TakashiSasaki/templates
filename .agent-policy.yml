schema_version: 2
toolchain:
  repository: TakashiSasaki/templates
  revision: b586d3caab11df3b7a396b202c2127d1f3453cfa
contexts:
  default:
    profiles:
    - core
    - security-baseline
    - pull-request
    project_policy:
      files:
      - policy/project.md
  review:
    profiles:
    - core
    - security-baseline
    - review
    project_policy:
      files:
      - policy/project.md
outputs:
  agents:
    enabled: true
    path: AGENTS.md
    context: default
    renderer: agents-md
  review-authority:
    enabled: true
    path: .review-authority/review-policy.md
    context: review
    renderer: policy-context-md
skills:
  enabled: []
