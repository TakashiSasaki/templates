# Skill assets

Place static files used to produce outputs here, such as document templates, configuration skeletons, or sample resources.

Assets are not automatically instructions. Document how they are used in `SKILL.md` or an applicable file under `references/`.

Delete this directory when the concrete skill has no assets.
