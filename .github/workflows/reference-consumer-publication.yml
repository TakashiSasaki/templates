name: Reference consumer publication compatibility
on:
  pull_request:
permissions:
  contents: read
jobs:
  compatibility:
    uses: TakashiSasaki/templates/.github/workflows/build-pages.yml@961014d00cb28f5cc3ee5e162b6eae4323df470a
    with:
      site_ref: 961014d00cb28f5cc3ee5e162b6eae4323df470a
      composition_ref: ${{ github.event.pull_request.head.sha }}
