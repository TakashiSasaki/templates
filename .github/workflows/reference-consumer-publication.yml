name: Reference consumer publication compatibility
on:
  pull_request:
permissions:
  contents: read
jobs:
  compatibility:
    uses: TakashiSasaki/templates/.github/workflows/build-pages.yml@0004413894a421bee4114eacd5a6885314b1d8bc
    with:
      site_ref: 0004413894a421bee4114eacd5a6885314b1d8bc
      composition_ref: ${{ github.event.pull_request.head.sha }}
      publication_staging_ids: composition-provider-maintenance,composition-installer-release
