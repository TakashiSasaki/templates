# Exact runtime distribution set for the Composition Composer.
#
# This lock is the consumer-runtime dependency contract for scripts/compose.py.
# It intentionally excludes repository-development, test, documentation, and
# publication-only dependencies. Install every entry unconditionally with
# dependency resolution disabled so supported Python versions receive the same
# reviewed runtime set.
#
# Update only through a reviewed dependency-resolution change.
attrs===26.1.0
jsonschema===4.26.0
jsonschema-specifications===2025.9.1
referencing===0.37.0
rpds-py===2026.6.3
typing_extensions===4.16.0
