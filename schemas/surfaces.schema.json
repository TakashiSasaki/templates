{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/TakashiSasaki/templates/blob/webapp/schemas/surfaces.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "surfaces"],
  "properties": {
    "$schema": {"type": "string"},
    "schemaVersion": {"const": 1},
    "surfaces": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "title",
          "purpose",
          "audiences",
          "authentication",
          "authorization",
          "dataClassifications",
          "stability",
          "startupDependencies",
          "diagnostic"
        ],
        "properties": {
          "id": {"type": "string", "pattern": "^[a-z][a-z0-9-]*$"},
          "title": {"type": "string", "minLength": 1},
          "purpose": {"type": "string", "minLength": 1},
          "audiences": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {"type": "string", "pattern": "^[a-z][a-z0-9-]*$"}
          },
          "authentication": {"enum": ["none", "optional", "required"]},
          "authorization": {
            "type": "object",
            "additionalProperties": false,
            "required": ["mode", "roles"],
            "properties": {
              "mode": {"enum": ["public", "authenticated", "role", "policy"]},
              "roles": {
                "type": "array",
                "uniqueItems": true,
                "items": {"type": "string", "pattern": "^[a-z][a-z0-9-]*$"}
              }
            }
          },
          "dataClassifications": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {"type": "string", "pattern": "^[a-z][a-z0-9-]*$"}
          },
          "stability": {"enum": ["experimental", "stable", "deprecated"]},
          "startupDependencies": {
            "type": "array",
            "uniqueItems": true,
            "items": {"type": "string", "pattern": "^[a-z][a-z0-9-]*$"}
          },
          "diagnostic": {"type": "boolean"}
        }
      }
    }
  }
}
