# Repository trees

These pages provide a complete, build-generated inventory of every tracked path
in the reviewed `skill`, `policy`, and `webapp` revisions. The Skill and Web
application publications also expose their `template/` subtrees as separate
copy-oriented views rooted at the files a template consumer receives.

For source-oriented browsing, use the [repository file browser](/files/). It
keeps an expandable tree beside the selected file, covers the `site`, `skill`,
`policy`, and `webapp` build inputs, and provides line-number and line-wrapping
toggles with build-time syntax highlighting for eligible UTF-8 text files.

The inventory is separate from the documentation catalog. Cataloged Markdown
links to its human-readable documentation page. Eligible regular UTF-8 text
files in the complete source views also receive a bounded, escaped inline
preview; all entries retain an immutable GitHub link at the exact rendered
commit. Binary, oversized, symlink, and gitlink entries remain GitHub-only in
the repository-tree preview surface.

<!-- GENERATED_REPOSITORY_TREE_INDEX -->

Additional copy boundaries:

| View | Revision | Directories | Regular files | Symlinks | Gitlinks | Documentation pages |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
<!-- GENERATED_SKILL_TEMPLATE_SUMMARY -->
<!-- GENERATED_WEBAPP_TEMPLATE_SUMMARY -->
