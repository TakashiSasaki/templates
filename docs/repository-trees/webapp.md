# Web application source and template tree

This page is generated from the exact `webapp` commit checked out by the site
build. The complete tree is the **template-development source artifact**. Its
`template/` subtree is the **copyable application-template distribution** whose
contents may be copied directly to a new product repository root.

A dedicated [copyable template tree](webapp/template.md) renders that subtree as
the consumer-facing repository root. Use this complete source view when
reviewing how the distribution is developed, validated, tested, and published.

The two roots have different ownership:

- paths below `template/` are downstream repository content;
- publication integration, distribution validation, source audits, clean-room
  fixture producers, and source-maintainer regressions remain outside
  `template/`.

The generated tree preserves this physical boundary. Directories are
collapsible, so the `template/` subtree can also be inspected in context.
Eligible regular UTF-8 text files can be opened in the sandboxed inline frame
while their immutable GitHub source links remain available. Binary files,
oversized files, symlinks, and gitlinks are not previewed; symlinks and gitlinks
are never followed.

<!-- GENERATED_REPOSITORY_TREE:webapp -->
