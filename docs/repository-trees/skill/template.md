# Skill copyable template tree

This page renders only the tracked contents below `skill/template/` at the exact
full commit SHA locked by the site build. It is the Skill-root view a template
consumer receives after:

```sh
cp -a template/. /path/to/new-skill/
```

Paths are displayed relative to that copy boundary while immutable GitHub links
still identify their canonical `template/...` source paths. Cataloged Markdown
files link to their published documentation destinations.

The complete [Skill source tree](../skill.md) remains available for reviewing
publication integration, source audits, distribution validation, canonical
fixtures, and source-maintainer tests. Inline previews are generated only once
in that complete source view to keep preview byte budgets closed; this
copy-oriented page retains immutable source links for every entry.

<!-- GENERATED_SKILL_TEMPLATE_TREE -->
